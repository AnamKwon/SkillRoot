interface:
  display_name: "Sensemaking"
  short_description: "Build plausible stories from messy cues"
  default_prompt: "Use $sensemaking to turn these logs, errors, and reports into a testable working story."
policy:
  allow_implicit_invocation: true
